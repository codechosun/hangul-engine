# 코딩 컨벤션

> 이 교재 전체와 엔진 코드가 따르는 규칙.
> 기반은 **언리얼 코딩 컨벤션**이되, 언리얼 전용 요소는 뺀 **범용 부분만** 채택함.

## 왜 언리얼 컨벤션인가

기존 세 권(C 문법 / 자료구조 / C++ 문법)이 이미 PascalCase를 쓰고 있음. 사실상 언리얼 스타일이었던 것을 정식으로 문서화하는 것뿐임.

문서화된 표준을 따르면 얻는 것 둘.

- 29장 내내 "이건 어떻게 쓰지"를 다시 고민하지 않음
- 독자가 나중에 실무에서 같은 규칙을 만남

---

## 채택하는 것

### 이름

| 대상 | 규칙 | 예 |
|---|---|---|
| 함수 | PascalCase | `CountBytes()`, `ReportResult()` |
| 변수 | PascalCase | `CharCount`, `Ratio` |
| 전역 변수 | **`G`** 접두사 | `GCheckCount`, `GFailCount` |
| 불리언 | **`b`** 접두사 | `bIsValid`, `bHasNext` |
| 구조체·클래스 | **`F`** 접두사 | `FHashMap`, `FTensor` |
| 열거형 | **`E`** 접두사 | `ETokenKind` |
| 템플릿 | **`T`** 접두사 (C++ 파트) | `TArray` |
| 매크로 | 전부 대문자 + 언더바 | `CHECK_NEAR` |
| 상수 | PascalCase | `MaxTokenCount` |

루프 변수 `i`, `j`는 예외로 소문자 허용.

### 서식

- **중괄호는 Allman** — 여는 괄호가 새 줄에
- 들여쓰기 **공백 4칸**
- 포인터 별표는 타입 쪽에 — `const char* Text`

```c
int CountBytes(const char* Text)
{
    int Count = 0;

    while (*Text != '\0')
    {
        Count++;
        Text++;
    }

    return Count;
}
```

### 불리언 접두사를 쓰는 이유

C에는 불리언 타입이 약함. `int bHasNext`를 보면 이게 **개수인지 참거짓인지** 이름만 보고 알 수 있음. C11에서는 `<stdbool.h>`의 `bool`을 쓰되 접두사는 그대로 붙임.

```c
bool bIsHangul = (Code >= 0xAC00 && Code <= 0xD7A3);   // 참거짓
int  HangulCount = 0;                                   // 개수
```

---

## 채택하지 않는 것

| 규칙 | 이유 |
|---|---|
| `U` 접두사 (UObject 파생) | 리플렉션 시스템이 없음. **의미 없는 장식** |
| `A` 접두사 (Actor 파생) | 게임 오브젝트 개념이 없음 |
| `I` 접두사 (인터페이스) | 언리얼 인터페이스 체계를 쓰지 않음 |

이 셋은 언리얼의 **런타임 구조에 묶인 표식**이라 그 구조가 없으면 붙일 근거가 없음.

---

## 문자 인코딩

**엔진 내부는 UTF-8로 고정.** 예외 없음.

이유 세 가지.

**① BPE가 Byte Pair Encoding임.** 현대 LLM 토크나이저는 **UTF-8 바이트 위에서 도는 알고리즘**이고, 어휘 자체가 바이트 시퀀스로 만들어짐. 다른 인코딩으로 들고 있어봐야 토큰화 직전에 UTF-8로 되돌려야 함.

**② 데이터가 전부 UTF-8임.** 학습 코퍼스, 어휘 파일, 모델 메타데이터 전부.

**③ 플랫폼 차이가 없음.** `wchar_t`는 윈도우에서 2바이트, 리눅스에서 4바이트라 코드가 갈림. UTF-8은 어디서나 같음.

### 소스 파일도 UTF-8

MSVC에 `/utf-8` 옵션을 줌 (A0 참고). 없으면 한글 주석과 문자열이 깨짐.

### 다른 인코딩과 만날 때

엔진 **가장자리에서 한 번만** 변환하고, 내부로는 UTF-8만 들여보냄.

```
[바깥 세계]  무슨 인코딩이든
     ↕  API 경계에서 1회 변환
[엔진 내부]  UTF-8 고정
```

---

## 오류 처리

**예외를 쓰지 않음.** C에는 없고, C++ 파트에서도 안 씀.

| 상황 | 방법 |
|---|---|
| 일어날 수 있는 오류 | 반환값으로 알림 |
| 일어나면 안 되는 상황 | `assert`로 즉시 중단 |

검증은 `CHECK` / `CHECK_NEAR` (A0에서 제작).

---

## 숫자 타입

실수는 `float`·`double`을 직접 쓰지 않고 **`Real`**을 씀 (`common/Types.h`).

`USE_DOUBLE`을 정의하면 `double`로 바뀜. 역전파를 수치미분으로 검증할 때 이 전환이 필요해짐.
